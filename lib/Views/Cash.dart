import 'package:flutter/material.dart';

class FlutterCash extends StatelessWidget {
  const FlutterCash({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: const Color(0xFF040C23),
    );
  }
}
